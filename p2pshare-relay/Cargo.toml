[package]
name = "p2pshare-relay"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "p2pshare-relay"
path = "src/main.rs"

[dependencies]
tokio  = { version = "1", features = ["full"] }
tracing            = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
